{
  "name": "timer",
  "version": "1.0.0",
  "description": "",
  "main": "process.js",
  "scripts": {
    "start": "npm run build",
    "build": "npm run export && npm run process",
    "export": "CURDIR=`pwd` && mkdir -p $CURDIR/tmp && /usr/bin/env osascript -l JavaScript scripts/export.jxa $CURDIR/tmp/output.json",
    "process": "node scripts/process.js ./tmp/output.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "colorette": "^1.0.7",
    "date-fns": "^1.30.1",
    "jsonfile": "^5.0.0",
    "lodash.groupby": "^4.6.0"
  }
}
